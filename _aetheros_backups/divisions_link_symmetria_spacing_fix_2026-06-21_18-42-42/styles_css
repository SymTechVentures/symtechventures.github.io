
:root{
  --bg:#040815; --panel:#0a1124; --panel2:#101936; --text:#f3f7ff; --soft:#c6d1e8;
  --muted:#8d9ab8; --line:rgba(140,180,255,.22); --cyan:#61eaff; --blue:#4ea3ff;
  --violet:#a77cff; --green:#8ff7c1; --gold:#ffd36a; --max:1120px;
}
*{box-sizing:border-box} html{scroll-behavior:smooth} body{margin:0;font-family:Inter,Segoe UI,system-ui,Arial,sans-serif;background:radial-gradient(circle at top,#101a38 0,#040815 46%,#02040c 100%);color:var(--text);line-height:1.65} img{max-width:100%;display:block} a{color:inherit;text-decoration:none}.wrap{width:min(calc(100% - 32px),var(--max));margin:auto}.topbar{position:sticky;top:0;z-index:10;background:rgba(4,8,21,.88);backdrop-filter:blur(14px);border-bottom:1px solid var(--line)}.navrow{height:82px;display:flex;align-items:center;justify-content:space-between;gap:22px}.brand{display:flex;align-items:center;gap:14px}.brand img{width:52px;height:52px;object-fit:contain;border-radius:14px}.brand strong{display:block;letter-spacing:.08em;text-transform:uppercase}.brand span{display:block;color:var(--muted);font-size:.8rem}.nav{display:flex;gap:8px;flex-wrap:wrap;justify-content:flex-end}.nav a{padding:9px 12px;border:1px solid transparent;border-radius:999px;color:var(--soft);font-size:.82rem;font-weight:700}.nav a:hover{border-color:var(--line);color:white;background:rgba(255,255,255,.04)}.hero{min-height:620px;display:grid;place-items:center;text-align:center;position:relative;overflow:hidden;border-bottom:1px solid var(--line)}.hero:before{content:"";position:absolute;inset:0;background:linear-gradient(rgba(4,8,21,.45),rgba(4,8,21,.92)),var(--hero) center/cover no-repeat;opacity:.92}.hero .inner{position:relative;padding:80px 0}.hero-logo{width:170px;margin:0 auto 24px;filter:drop-shadow(0 0 24px rgba(97,234,255,.25))}.eyebrow{color:var(--cyan);font-weight:800;letter-spacing:.18em;text-transform:uppercase;font-size:.8rem}.hero h1,.page-hero h1{font-size:clamp(2.3rem,6vw,5.1rem);line-height:1.04;margin:.3rem 0 1rem;letter-spacing:.05em;text-transform:uppercase}.lead{font-size:clamp(1.08rem,2vw,1.35rem);color:var(--soft);max-width:820px;margin:0 auto}.actions{display:flex;justify-content:center;gap:14px;flex-wrap:wrap;margin-top:28px}.btn{border:1px solid var(--line);border-radius:999px;padding:12px 18px;font-weight:800;background:rgba(255,255,255,.05)}.btn.primary{background:linear-gradient(135deg,rgba(78,163,255,.95),rgba(167,124,255,.82));border-color:transparent}.btn:hover{transform:translateY(-1px);box-shadow:0 14px 34px rgba(0,0,0,.22)}section{padding:72px 0}.section-head{text-align:center;max-width:850px;margin:0 auto 32px}.section-head h2{font-size:clamp(1.9rem,4vw,3.1rem);line-height:1.08;margin:.2rem 0 1rem}.section-head p{color:var(--soft);font-size:1.08rem}.grid{display:grid;gap:20px}.cards-3{grid-template-columns:repeat(3,1fr)}.cards-5{grid-template-columns:repeat(5,1fr)}.card{background:linear-gradient(180deg,rgba(255,255,255,.055),rgba(255,255,255,.025));border:1px solid var(--line);border-radius:24px;padding:24px;text-align:center;box-shadow:0 22px 60px rgba(0,0,0,.18)}.card img.icon{width:92px;height:92px;object-fit:contain;margin:0 auto 16px;border-radius:24px}.card h3{margin:.2rem 0 .5rem;font-size:1.2rem}.card p{color:var(--soft);margin:0}.card a{display:inline-block;margin-top:16px;color:var(--cyan);font-weight:800}.split{display:grid;grid-template-columns:1.05fr .95fr;gap:34px;align-items:center}.panel{background:rgba(10,17,36,.72);border:1px solid var(--line);border-radius:28px;padding:30px;text-align:center}.panel p{color:var(--soft)}.feature-img{border-radius:28px;border:1px solid var(--line);min-height:280px;width:100%;object-fit:cover}.banner{height:270px;border-radius:28px;border:1px solid var(--line);object-fit:cover;width:100%;margin-bottom:28px}.page-hero{padding:86px 0 50px;text-align:center;border-bottom:1px solid var(--line);background:radial-gradient(circle at top,rgba(78,163,255,.12),transparent 55%)}.page-logo{width:128px;margin:0 auto 18px}.timeline{display:grid;gap:14px;max-width:820px;margin:0 auto}.step{display:grid;grid-template-columns:70px 1fr;gap:18px;align-items:start;text-align:left;background:rgba(255,255,255,.045);border:1px solid var(--line);border-radius:20px;padding:18px}.step strong{display:grid;place-items:center;width:52px;height:52px;border-radius:16px;background:rgba(97,234,255,.1);color:var(--cyan)}.step h3{margin:0 0 .25rem}.step p{margin:0;color:var(--soft)}.notice{border:1px solid rgba(255,211,106,.35);background:rgba(255,211,106,.065);border-radius:24px;padding:24px;text-align:center;max-width:900px;margin:0 auto}.footer{padding:36px 0;border-top:1px solid var(--line);text-align:center;color:var(--muted)}.footer a{color:var(--soft)}.simple-list{max-width:780px;margin:0 auto;text-align:center;color:var(--soft)}.simple-list strong{color:var(--text)}
@media(max-width:900px){.navrow{height:auto;padding:14px 0;align-items:flex-start;flex-direction:column}.nav{justify-content:flex-start}.split,.cards-3,.cards-5{grid-template-columns:1fr}.hero{min-height:560px}.card{padding:22px}.step{grid-template-columns:1fr;text-align:center}.step strong{margin:auto}}

/* AETHEROS_SHIMMER_HARD_RESET_START */
html,
body {
  margin: 0 !important;
  padding: 0 !important;
  min-height: 100%;
}

body {
  position: relative !important;
  overflow-x: hidden !important;
  background:
    radial-gradient(ellipse at 12% 8%, rgba(75, 210, 255, 0.13), transparent 34rem),
    radial-gradient(ellipse at 88% 18%, rgba(167, 124, 255, 0.11), transparent 40rem),
    radial-gradient(ellipse at 48% 92%, rgba(255, 211, 106, 0.06), transparent 44rem),
    linear-gradient(180deg, #050917 0%, #030610 56%, #01030a 100%) !important;
}

#aetheros-starfield,
#aetheros-shimmer-layer {
  position: fixed !important;
  inset: 0 !important;
  width: 100vw !important;
  height: 100vh !important;
  display: block !important;
  pointer-events: none !important;
  margin: 0 !important;
  padding: 0 !important;
  border: 0 !important;
  max-width: none !important;
  max-height: none !important;
  mix-blend-mode: screen !important;
}

#aetheros-starfield {
  z-index: 0 !important;
  opacity: 1 !important;
}

#aetheros-shimmer-layer {
  z-index: 1 !important;
  opacity: 0.82 !important;
}

body > header,
body > main,
body > footer,
header,
main,
footer,
.topbar,
.site-header,
.wrap,
.hero,
.page-hero,
.card,
.panel,
.notice,
.step {
  position: relative;
  z-index: 2;
}

header,
.topbar,
.site-header,
.hero,
.page-hero {
  margin-top: 0 !important;
}
/* AETHEROS_SHIMMER_HARD_RESET_END */

/* AETHEROS_NSRP_PRELUDE_SERIES_START */
.nsrp-prelude-hero { min-height: 58vh; }
.prelude-intro, .prelude-series, .prelude-feature, .prelude-caution { position: relative; }
.prelude-panel, .feature-panel { border: 1px solid rgba(118,226,255,.28); box-shadow: 0 24px 80px rgba(0,0,0,.32), 0 0 42px rgba(118,226,255,.08); }
.section-lead { max-width: 780px; color: rgba(232,241,255,.76); font-size: 1.05rem; line-height: 1.7; }
.topic-category { margin-top: 3rem; }
.topic-category > h3 { margin-bottom: .35rem; color: #eaf7ff; }
.topic-category > p { margin-top: 0; color: rgba(225,236,255,.72); }
.topic-grid { display: grid; grid-template-columns: repeat(3,minmax(0,1fr)); gap: 1rem; margin-top: 1.2rem; }
.topic-card { position: relative; padding: 1.25rem; border: 1px solid rgba(132,190,255,.26); border-radius: 18px; background: linear-gradient(180deg, rgba(255,255,255,.075), rgba(255,255,255,.028)); box-shadow: 0 18px 52px rgba(0,0,0,.28), inset 0 1px 0 rgba(255,255,255,.055); overflow: hidden; }
.topic-card::before { content: ""; position: absolute; inset: 0; background: radial-gradient(circle at 20% 0%, rgba(118,226,255,.15), transparent 38%), radial-gradient(circle at 88% 100%, rgba(167,124,255,.12), transparent 42%); pointer-events: none; opacity: .85; }
.topic-card > * { position: relative; z-index: 1; }
.topic-card h4 { margin: .8rem 0 .65rem; color: #fff; line-height: 1.25; }
.topic-card p { color: rgba(232,241,255,.74); line-height: 1.58; }
.week-label { display: inline-flex; font-size: .78rem; letter-spacing: .08em; text-transform: uppercase; color: #8eeaff; }
.status-badge { display: inline-flex; margin-top: .75rem; padding: .32rem .65rem; border-radius: 999px; font-size: .76rem; letter-spacing: .04em; text-transform: uppercase; border: 1px solid rgba(255,255,255,.16); }
.status-badge.complete { color: #dffcff; background: rgba(77,220,255,.12); border-color: rgba(77,220,255,.32); }
.status-badge.planned { color: rgba(232,241,255,.72); background: rgba(255,255,255,.055); }
.hero-actions { display: flex; flex-wrap: wrap; gap: .85rem; margin-top: 1.4rem; }
.button.secondary { background: rgba(255,255,255,.065); border-color: rgba(255,255,255,.22); }
@media (max-width: 980px) { .topic-grid { grid-template-columns: 1fr 1fr; } }
@media (max-width: 680px) { .topic-grid { grid-template-columns: 1fr; } .nsrp-prelude-hero { min-height: auto; } }
/* AETHEROS_NSRP_PRELUDE_SERIES_END */

/* AETHEROS_NAV_AND_STARFIELD_CONSISTENCY_START */
:root {
  --aetheros-surface-strong: rgba(5, 10, 24, 0.78);
  --aetheros-surface-medium: rgba(5, 10, 24, 0.62);
  --aetheros-surface-soft: rgba(5, 10, 24, 0.42);
  --aetheros-border-soft: rgba(132, 190, 255, 0.24);
}

/* Keep the starfield consistently visible behind the whole page. */
body,
main,
section,
.page,
.page-hero,
.hero,
.prelude-intro,
.prelude-series,
.prelude-feature,
.prelude-caution {
  background-color: transparent !important;
}

/* Avoid fully opaque blocks that make the starfield vanish in some sections. */
.panel,
.card,
.notice,
.step,
.division-card,
.topic-card,
.feature-panel,
.prelude-panel,
[class*="card"] {
  background:
    linear-gradient(180deg, rgba(255,255,255,0.070), rgba(255,255,255,0.026)),
    var(--aetheros-surface-medium) !important;
  border-color: var(--aetheros-border-soft) !important;
  backdrop-filter: blur(10px);
}

/* Header/footer need slightly more density for readability, but still not flat black. */
.topbar,
.site-header,
.footer {
  background: var(--aetheros-surface-strong) !important;
  backdrop-filter: blur(14px);
}

/* Some page sections may have inherited solid backgrounds. Neutralise those without changing layout. */
section::before,
section::after {
  pointer-events: none;
}

/* Return/back navigation. */
.page-return-bar {
  position: relative;
  z-index: 3;
  background: rgba(5, 10, 24, 0.46);
  border-bottom: 1px solid rgba(132, 190, 255, 0.16);
  backdrop-filter: blur(10px);
}

.page-return-inner {
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 0.85rem;
  padding-top: 0.7rem;
  padding-bottom: 0.7rem;
}

.page-return-button,
.page-return-home {
  display: inline-flex;
  align-items: center;
  justify-content: center;
  min-height: 2.35rem;
  padding: 0.55rem 0.9rem;
  border-radius: 999px;
  border: 1px solid rgba(132, 190, 255, 0.26);
  background: rgba(255, 255, 255, 0.055);
  color: rgba(235, 247, 255, 0.92);
  text-decoration: none;
  font: inherit;
  cursor: pointer;
  transition: border-color 180ms ease, background 180ms ease, transform 180ms ease;
}

.page-return-button:hover,
.page-return-home:hover {
  transform: translateY(-1px);
  border-color: rgba(118, 226, 255, 0.54);
  background: rgba(118, 226, 255, 0.10);
}

@media (max-width: 640px) {
  .page-return-inner {
    padding-top: 0.55rem;
    padding-bottom: 0.55rem;
  }

  .page-return-button,
  .page-return-home {
    min-height: 2.2rem;
    padding: 0.48rem 0.75rem;
  }
}
/* AETHEROS_NAV_AND_STARFIELD_CONSISTENCY_END */

/* AETHEROS_NSRP_WEEK01_LINKS_START */
.compact-prelude-panel {
  max-width: 980px;
  margin-inline: auto;
}

.featured-topic {
  border-color: rgba(118, 226, 255, 0.46) !important;
  box-shadow:
    0 22px 70px rgba(0, 0, 0, 0.34),
    inset 0 1px 0 rgba(255,255,255,0.07),
    0 0 42px rgba(118, 226, 255, 0.11) !important;
}

.topic-actions {
  display: flex;
  flex-wrap: wrap;
  gap: 0.65rem;
  margin-top: 1rem;
}

.mini-button {
  display: inline-flex;
  align-items: center;
  justify-content: center;
  min-height: 2.15rem;
  padding: 0.45rem 0.78rem;
  border-radius: 999px;
  border: 1px solid rgba(118, 226, 255, 0.42);
  background: rgba(118, 226, 255, 0.12);
  color: rgba(239, 250, 255, 0.96);
  text-decoration: none;
  font-size: 0.82rem;
  letter-spacing: 0.02em;
  transition: transform 180ms ease, border-color 180ms ease, background 180ms ease;
}

.mini-button:hover {
  transform: translateY(-1px);
  border-color: rgba(118, 226, 255, 0.74);
  background: rgba(118, 226, 255, 0.18);
}

.mini-button.secondary {
  border-color: rgba(190, 150, 255, 0.38);
  background: rgba(190, 150, 255, 0.10);
}

.paper-feature-panel {
  margin-top: 1.5rem;
}
/* AETHEROS_NSRP_WEEK01_LINKS_END */

/* AETHEROS_ROUNDED_GLASS_CARDS_START */
/*
  Purpose:
  - Restore rounded card/panel boxes.
  - Keep them semi-transparent.
  - Avoid the rectangular compositing artefacts caused by broad grid-child blur rules.
  - Do not modify the starfield or shimmer.
*/

:root {
  --symtech-card-glass-bg: rgba(8, 16, 36, 0.34);
  --symtech-card-glass-bg-hover: rgba(10, 22, 48, 0.44);
  --symtech-card-border: rgba(126, 210, 255, 0.16);
  --symtech-card-border-hover: rgba(126, 210, 255, 0.28);
}

/* Layout containers must stay invisible so no large black rectangles return */
.grid,
.cards,
.cards-3,
.cards-5,
.card-grid,
.topic-grid,
.section-grid,
.wrap,
section,
main,
.page-section,
.content-section,
.home-section,
.divisions-section,
.founding-section,
.research-section,
.prelude-preview,
.prelude-series,
.prelude-intro,
.prelude-feature,
.prelude-paper-feature,
.prelude-caution {
  background: transparent !important;
  background-color: transparent !important;
  background-image: none !important;
  border: 0 !important;
  outline: 0 !important;
  box-shadow: none !important;
  filter: none !important;
  backdrop-filter: none !important;
  -webkit-backdrop-filter: none !important;
}

/* No pseudo-elements on layout containers */
.grid::before,
.grid::after,
.cards::before,
.cards::after,
.cards-3::before,
.cards-3::after,
.cards-5::before,
.cards-5::after,
.card-grid::before,
.card-grid::after,
.topic-grid::before,
.topic-grid::after,
.section-grid::before,
.section-grid::after,
.wrap::before,
.wrap::after,
section::before,
section::after,
main::before,
main::after {
  content: none !important;
  display: none !important;
}

/* Restore the actual rounded boxes only on real content cards/panels */
.card,
.panel,
.notice,
.step,
.topic-card,
.feature-panel,
.prelude-panel,
.paper-feature-panel,
.compact-prelude-panel,
.featured-topic {
  background:
    linear-gradient(180deg, rgba(255,255,255,0.055), rgba(255,255,255,0.014)),
    var(--symtech-card-glass-bg) !important;
  background-color: var(--symtech-card-glass-bg) !important;
  border: 1px solid var(--symtech-card-border) !important;
  border-radius: 24px !important;
  outline: 0 !important;
  box-shadow:
    inset 0 1px 0 rgba(255,255,255,0.055),
    0 12px 30px rgba(0,0,0,0.14) !important;
  filter: none !important;
  backdrop-filter: none !important;
  -webkit-backdrop-filter: none !important;
  overflow: hidden !important;
}

/* Special timeline cards retain a slightly smaller radius */
.step {
  border-radius: 20px !important;
}

/* Topic cards can keep their shape but not their old pseudo glow layer */
.topic-card,
.featured-topic {
  border-radius: 18px !important;
}

/* Keep pseudo-layers disabled because they caused edge/box artefacts */
.card::before,
.card::after,
.panel::before,
.panel::after,
.notice::before,
.notice::after,
.step::before,
.step::after,
.topic-card::before,
.topic-card::after,
.feature-panel::before,
.feature-panel::after,
.prelude-panel::before,
.prelude-panel::after,
.paper-feature-panel::before,
.paper-feature-panel::after,
.compact-prelude-panel::before,
.compact-prelude-panel::after,
.featured-topic::before,
.featured-topic::after {
  content: none !important;
  display: none !important;
  background: transparent !important;
  background-color: transparent !important;
  background-image: none !important;
  border: 0 !important;
  outline: 0 !important;
  box-shadow: none !important;
  opacity: 0 !important;
}

/* Hover restores a little life without creating hard rectangles */
.card:hover,
.panel:hover,
.notice:hover,
.step:hover,
.topic-card:hover,
.feature-panel:hover,
.prelude-panel:hover,
.paper-feature-panel:hover,
.compact-prelude-panel:hover,
.featured-topic:hover {
  background:
    linear-gradient(180deg, rgba(255,255,255,0.070), rgba(255,255,255,0.020)),
    var(--symtech-card-glass-bg-hover) !important;
  background-color: var(--symtech-card-glass-bg-hover) !important;
  border-color: var(--symtech-card-border-hover) !important;
  box-shadow:
    inset 0 1px 0 rgba(255,255,255,0.070),
    0 16px 36px rgba(0,0,0,0.18) !important;
  transform: translateY(-1px);
  filter: none !important;
  backdrop-filter: none !important;
  -webkit-backdrop-filter: none !important;
}

/* Text readability */
.card p,
.panel p,
.notice p,
.step p,
.topic-card p,
.feature-panel p,
.prelude-panel p,
.paper-feature-panel p,
.compact-prelude-panel p,
.featured-topic p {
  color: rgba(232, 241, 255, 0.86) !important;
  text-shadow: 0 1px 2px rgba(0, 0, 0, 0.32);
}

.card h1, .card h2, .card h3, .card h4,
.panel h1, .panel h2, .panel h3, .panel h4,
.notice h1, .notice h2, .notice h3, .notice h4,
.step h1, .step h2, .step h3, .step h4,
.topic-card h1, .topic-card h2, .topic-card h3, .topic-card h4,
.feature-panel h1, .feature-panel h2, .feature-panel h3, .feature-panel h4,
.prelude-panel h1, .prelude-panel h2, .prelude-panel h3, .prelude-panel h4,
.paper-feature-panel h1, .paper-feature-panel h2, .paper-feature-panel h3, .paper-feature-panel h4,
.compact-prelude-panel h1, .compact-prelude-panel h2, .compact-prelude-panel h3, .compact-prelude-panel h4,
.featured-topic h1, .featured-topic h2, .featured-topic h3, .featured-topic h4 {
  color: rgba(248, 252, 255, 0.98) !important;
  text-shadow: 0 1px 3px rgba(0, 0, 0, 0.44);
}

/* Header/footer/nav remain readable */
.topbar,
.site-header,
.footer {
  background: rgba(3, 8, 22, 0.44) !important;
  border-color: rgba(118, 226, 255, 0.08) !important;
  box-shadow: none !important;
}

.page-return-bar {
  background: rgba(3, 8, 22, 0.18) !important;
  border-bottom: 0 !important;
  box-shadow: none !important;
}
/* AETHEROS_ROUNDED_GLASS_CARDS_END */


/* AETHEROS_HIDE_NSRP_SECTIONS_START */

/* Hide redundant NSRP prelude sections */
.page-return-bar{
    display:none !important;
}

.prelude-feature{
    display:none !important;
}

.prelude-paper-feature{
    display:none !important;
}

/* AETHEROS_HIDE_NSRP_SECTIONS_END */


/* AETHEROS_STICKY_HEADER_START */

.site-header,
.topbar,
header.site-header,
header.topbar{
    position: sticky !important;
    top: 0 !important;
    z-index: 9999 !important;
}

/* AETHEROS_STICKY_HEADER_END */


/* AETHEROS_HIDE_NSRP_HERO_ACTIONS_START */

.nsrp-prelude-hero .hero-actions{
    display:none !important;
}

/* AETHEROS_HIDE_NSRP_HERO_ACTIONS_END */


/* AETHEROS_TARGETED_HEADER_NORMALISE_START */

.topbar,
header.topbar{
    position: sticky !important;
    top: 0 !important;
    z-index: 99999 !important;
}

.nsrp-prelude-hero .hero-actions{
    display:none !important;
}

.prelude-feature,
.prelude-paper-feature{
    display:none !important;
}

/* Keep old injected nav systems suppressed if any remain */
.aetheros-universal-nav,
.page-return-bar{
    display:none !important;
}

/* Help standalone paper pages accept the normal site header */
body > header.topbar{
    margin-top:0 !important;
}

/* AETHEROS_TARGETED_HEADER_NORMALISE_END */


/* AETHEROS_PROMOTE_PAPER_HEADER_GLOBAL_START */

/* Promote the larger/brighter paper-style header to the whole website. */
.topbar,
header.topbar{
  position:sticky !important;
  top:0 !important;
  z-index:99999 !important;
  min-height:78px !important;
  width:100% !important;
  max-width:100% !important;
  margin:0 !important;
  padding:0 !important;
  box-sizing:border-box !important;
  background:rgba(3,8,22,0.94) !important;
  border-bottom:1px solid rgba(126,210,255,0.18) !important;
  box-shadow:0 8px 28px rgba(0,0,0,0.22) !important;
  backdrop-filter:blur(10px);
  -webkit-backdrop-filter:blur(10px);
}

.topbar,
.topbar *{
  box-sizing:border-box !important;
  text-align:left !important;
  text-justify:auto !important;
  hyphens:none !important;
  -webkit-hyphens:none !important;
  word-break:normal !important;
}

.topbar .wrap.navrow,
.topbar .wrap.nav-wrap,
.topbar .navrow,
.topbar .nav-wrap{
  max-width:1200px !important;
  width:100% !important;
  min-height:78px !important;
  margin:0 auto !important;
  padding:14px 24px !important;
  display:flex !important;
  align-items:center !important;
  justify-content:space-between !important;
  gap:26px !important;
}

.topbar .brand{
  display:flex !important;
  align-items:center !important;
  gap:14px !important;
  color:#f5f8ff !important;
  text-decoration:none !important;
  min-width:0 !important;
  flex:0 1 auto !important;
}

.topbar .brand img{
  width:50px !important;
  height:50px !important;
  object-fit:contain !important;
  display:block !important;
  flex:0 0 auto !important;
  margin:0 !important;
  padding:0 !important;
  border:0 !important;
}

.topbar .brand > span{
  display:flex !important;
  flex-direction:column !important;
  gap:3px !important;
  margin:0 !important;
  padding:0 !important;
  color:#f5f8ff !important;
}

.topbar .brand strong{
  display:block !important;
  font-size:1.08rem !important;
  font-weight:800 !important;
  line-height:1.08 !important;
  color:#ffffff !important;
  letter-spacing:0 !important;
  text-transform:none !important;
  white-space:nowrap !important;
  margin:0 !important;
  padding:0 !important;
}

.topbar .brand span span{
  display:block !important;
  font-size:0.84rem !important;
  font-weight:400 !important;
  line-height:1.08 !important;
  color:rgba(232,241,255,0.78) !important;
  letter-spacing:0 !important;
  text-transform:none !important;
  white-space:nowrap !important;
  margin:0 !important;
  padding:0 !important;
}

.topbar .nav{
  display:flex !important;
  align-items:center !important;
  justify-content:flex-end !important;
  gap:22px !important;
  flex-wrap:wrap !important;
  min-width:0 !important;
  margin:0 !important;
  padding:0 !important;
}

.topbar .nav a{
  display:inline-block !important;
  color:rgba(232,241,255,0.94) !important;
  text-decoration:none !important;
  font-size:1rem !important;
  font-weight:700 !important;
  line-height:1.1 !important;
  letter-spacing:0 !important;
  text-transform:none !important;
  white-space:nowrap !important;
  margin:0 !important;
  padding:0 !important;
  border:0 !important;
  background:transparent !important;
  box-shadow:none !important;
}

.topbar .nav a:hover{
  color:#5be7ff !important;
}

@media (max-width:900px){
  .topbar .wrap.navrow,
  .topbar .wrap.nav-wrap,
  .topbar .navrow,
  .topbar .nav-wrap{
    align-items:flex-start !important;
    flex-direction:column !important;
    gap:12px !important;
    padding:14px 20px !important;
  }

  .topbar .nav{
    justify-content:flex-start !important;
    gap:14px !important;
  }
}

/* AETHEROS_PROMOTE_PAPER_HEADER_GLOBAL_END */

/* AETHEROS HERO LOGO 220PX UNIFY START */
.page-logo,
.hero-icon,
.aetheros-fixed-hero-icon {
    width:220px !important;
    height:220px !important;
    max-width:220px !important;
    max-height:220px !important;
    object-fit:contain !important;
    display:block !important;
    margin:0 auto 1.35rem auto !important;
    filter:drop-shadow(0 0 24px rgba(95,210,255,0.34));
}

.hero-logo,
.home-hero-logo,
.hero img.hero-logo,
.hero img.home-hero-logo {
    width:220px !important;
    height:220px !important;
    max-width:220px !important;
    max-height:220px !important;
    object-fit:contain !important;
    display:block !important;
    margin:0 auto 1.35rem auto !important;
}

.aetheros-fixed-hero-icon[alt*="Divisions"],
.hero-icon[alt*="Divisions"],
img[alt="Divisions icon"] {
    transform:scale(1.05) !important;
}

@media (max-width: 900px) {
    .page-logo,
    .hero-icon,
    .aetheros-fixed-hero-icon,
    .hero-logo,
    .home-hero-logo,
    .hero img.hero-logo,
    .hero img.home-hero-logo {
        width:180px !important;
        height:180px !important;
        max-width:180px !important;
        max-height:180px !important;
        margin-bottom:1.05rem !important;
    }
}

@media (max-width: 560px) {
    .page-logo,
    .hero-icon,
    .aetheros-fixed-hero-icon,
    .hero-logo,
    .home-hero-logo,
    .hero img.hero-logo,
    .hero img.home-hero-logo {
        width:142px !important;
        height:142px !important;
        max-width:142px !important;
        max-height:142px !important;
        margin-bottom:0.85rem !important;
    }
}
/* AETHEROS HERO LOGO 220PX UNIFY END */


/* AETHEROS HOMEPAGE HERO LOGO ALIGNMENT START */
.home-hero-logo,
.hero-logo {
    position: relative !important;
    top: -32px !important;
}
/* AETHEROS HOMEPAGE HERO LOGO ALIGNMENT END */

/* AETHEROS FINAL CONTACT BANNER MATCH START */
body.contact-page .page-hero {
    padding: 86px 0 50px !important;
    text-align: center !important;
    border-bottom: 1px solid var(--line) !important;
    background: radial-gradient(circle at top,rgba(78,163,255,.12),transparent 55%) !important;
    min-height: auto !important;
    display: block !important;
    place-items: unset !important;
}

body.contact-page .page-hero .wrap {
    width: min(calc(100% - 32px), var(--max)) !important;
    margin: auto !important;
    padding: 0 !important;
    transform: none !important;
}

body.contact-page .page-hero .hero-icon,
body.contact-page .page-hero .aetheros-fixed-hero-icon,
body.contact-page .page-hero img[alt="Contact icon"] {
    width: 220px !important;
    height: 220px !important;
    max-width: 220px !important;
    max-height: 220px !important;
    object-fit: contain !important;
    display: block !important;
    margin: 0 auto 1.35rem auto !important;
    transform: none !important;
}

body.contact-page .page-hero .eyebrow {
    color: var(--cyan) !important;
    font-weight: 800 !important;
    letter-spacing: .18em !important;
    text-transform: uppercase !important;
    font-size: .8rem !important;
    margin: 0 0 .8rem 0 !important;
}

body.contact-page .page-hero h1 {
    font-size: clamp(2.3rem,6vw,5.1rem) !important;
    line-height: 1.04 !important;
    margin: .3rem 0 1rem !important;
    letter-spacing: .05em !important;
    text-transform: uppercase !important;
}

body.contact-page .page-hero .lead {
    font-size: clamp(1.08rem,2vw,1.35rem) !important;
    color: var(--soft) !important;
    max-width: 820px !important;
    margin: 0 auto !important;
}

@media (max-width: 900px) {
    body.contact-page .page-hero .hero-icon,
    body.contact-page .page-hero .aetheros-fixed-hero-icon,
    body.contact-page .page-hero img[alt="Contact icon"] {
        width: 180px !important;
        height: 180px !important;
        max-width: 180px !important;
        max-height: 180px !important;
    }
}

@media (max-width: 560px) {
    body.contact-page .page-hero .hero-icon,
    body.contact-page .page-hero .aetheros-fixed-hero-icon,
    body.contact-page .page-hero img[alt="Contact icon"] {
        width: 142px !important;
        height: 142px !important;
        max-width: 142px !important;
        max-height: 142px !important;
    }
}
/* AETHEROS FINAL CONTACT BANNER MATCH END */

/* AETHEROS FINAL FIXED HEADER SYSTEM START */
html {
    margin: 0 !important;
    padding: 0 !important;
    overflow-x: hidden !important;
}

body {
    margin: 0 !important;
    padding-top: 82px !important;
    overflow-x: hidden !important;
}

.aetheros-site-header {
    position: fixed !important;
    top: 0 !important;
    left: 0 !important;
    right: 0 !important;
    z-index: 2147483000 !important;

    width: 100vw !important;
    max-width: 100vw !important;
    height: 82px !important;
    min-height: 82px !important;
    max-height: 82px !important;

    margin: 0 !important;
    padding: 0 1.45rem !important;
    box-sizing: border-box !important;

    display: flex !important;
    flex-direction: row !important;
    align-items: center !important;
    justify-content: space-between !important;
    flex-wrap: nowrap !important;
    gap: 1rem !important;

    background: rgba(3, 8, 20, 0.965) !important;
    border: 0 !important;
    border-bottom: 1px solid rgba(120,180,255,0.18) !important;

    backdrop-filter: blur(14px) !important;
    -webkit-backdrop-filter: blur(14px) !important;

    transform: none !important;
    contain: none !important;
}

.aetheros-brand {
    display: flex !important;
    align-items: center !important;
    justify-content: flex-start !important;
    flex: 0 0 auto !important;

    height: 82px !important;
    min-height: 82px !important;
    max-height: 82px !important;

    gap: 0.75rem !important;
    min-width: 0 !important;
    max-width: 42vw !important;

    margin: 0 !important;
    padding: 0 !important;

    color: inherit !important;
    text-decoration: none !important;
    background: transparent !important;
    border: 0 !important;
}

.aetheros-brand-logo {
    width: 54px !important;
    height: 54px !important;
    min-width: 54px !important;
    min-height: 54px !important;
    max-width: 54px !important;
    max-height: 54px !important;

    object-fit: contain !important;
    display: block !important;
    flex: 0 0 auto !important;

    margin: 0 !important;
    padding: 0 !important;

    border-radius: 0 !important;
    background: transparent !important;
    box-shadow: none !important;
}

.aetheros-brand-text {
    display: flex !important;
    flex-direction: column !important;
    justify-content: center !important;

    min-width: 0 !important;
    height: auto !important;

    margin: 0 !important;
    padding: 0 !important;

    line-height: 1.08 !important;
    white-space: nowrap !important;
    overflow: hidden !important;
}

.aetheros-brand-text strong {
    display: block !important;
    color: #ffffff !important;

    font-size: 1.15rem !important;
    font-weight: 800 !important;
    line-height: 1.05 !important;
    letter-spacing: -0.02em !important;
    text-transform: none !important;

    margin: 0 !important;
    padding: 0 !important;
}

.aetheros-brand-text small {
    display: block !important;
    color: #b8c1d1 !important;

    font-size: 0.82rem !important;
    font-weight: 400 !important;
    line-height: 1.15 !important;
    letter-spacing: 0 !important;
    text-transform: none !important;

    margin: 0.12rem 0 0 0 !important;
    padding: 0 !important;
}

.aetheros-icon-nav {
    display: flex !important;
    flex-direction: row !important;
    align-items: center !important;
    justify-content: flex-end !important;
    flex-wrap: nowrap !important;
    flex: 1 1 auto !important;

    height: 82px !important;
    min-height: 82px !important;
    max-height: 82px !important;

    min-width: 0 !important;
    gap: 0.55rem !important;

    margin: 0 !important;
    padding: 0 !important;

    overflow-x: auto !important;
    overflow-y: hidden !important;
    white-space: nowrap !important;
    scrollbar-width: none !important;
    -ms-overflow-style: none !important;
}

.aetheros-icon-nav::-webkit-scrollbar {
    display: none !important;
}

.aetheros-nav-link {
    display: inline-flex !important;
    align-items: center !important;
    justify-content: center !important;
    flex: 0 0 auto !important;

    width: 44px !important;
    height: 44px !important;
    min-width: 44px !important;
    min-height: 44px !important;
    max-width: 44px !important;
    max-height: 44px !important;

    margin: 0 !important;
    padding: 0 !important;

    color: inherit !important;
    text-decoration: none !important;
    background: transparent !important;
    border: 0 !important;
    border-radius: 999px !important;
}

.aetheros-nav-link img {
    width: 38px !important;
    height: 38px !important;
    min-width: 38px !important;
    min-height: 38px !important;
    max-width: 38px !important;
    max-height: 38px !important;

    object-fit: contain !important;
    display: block !important;

    margin: 0 !important;
    padding: 0 !important;

    filter: drop-shadow(0 0 8px rgba(95,210,255,0.30));
}

body > .topbar:not(.aetheros-site-header),
body > .site-header:not(.aetheros-site-header),
body > header.topbar:not(.aetheros-site-header),
body > header.site-header:not(.aetheros-site-header) {
    display: none !important;
}

@media (max-width: 900px) {
    body {
        padding-top: 68px !important;
    }

    .aetheros-site-header {
        height: 68px !important;
        min-height: 68px !important;
        max-height: 68px !important;
        padding: 0 0.75rem !important;
        gap: 0.45rem !important;
    }

    .aetheros-brand {
        height: 68px !important;
        min-height: 68px !important;
        max-height: 68px !important;
        gap: 0.45rem !important;
        max-width: 34vw !important;
    }

    .aetheros-brand-logo {
        width: 42px !important;
        height: 42px !important;
        min-width: 42px !important;
        min-height: 42px !important;
        max-width: 42px !important;
        max-height: 42px !important;
    }

    .aetheros-brand-text strong {
        font-size: 0.86rem !important;
    }

    .aetheros-brand-text small {
        font-size: 0.64rem !important;
    }

    .aetheros-icon-nav {
        height: 68px !important;
        min-height: 68px !important;
        max-height: 68px !important;
        gap: 0.25rem !important;
    }

    .aetheros-nav-link {
        width: 34px !important;
        height: 34px !important;
        min-width: 34px !important;
        min-height: 34px !important;
        max-width: 34px !important;
        max-height: 34px !important;
    }

    .aetheros-nav-link img {
        width: 30px !important;
        height: 30px !important;
        min-width: 30px !important;
        min-height: 30px !important;
        max-width: 30px !important;
        max-height: 30px !important;
    }
}

@media (max-width: 560px) {
    .aetheros-brand-text {
        display: none !important;
    }

    .aetheros-brand {
        max-width: none !important;
    }

    .aetheros-brand-logo {
        width: 40px !important;
        height: 40px !important;
        min-width: 40px !important;
        min-height: 40px !important;
        max-width: 40px !important;
        max-height: 40px !important;
    }

    .aetheros-nav-link {
        width: 31px !important;
        height: 31px !important;
        min-width: 31px !important;
        min-height: 31px !important;
        max-width: 31px !important;
        max-height: 31px !important;
    }

    .aetheros-nav-link img {
        width: 28px !important;
        height: 28px !important;
        min-width: 28px !important;
        min-height: 28px !important;
        max-width: 28px !important;
        max-height: 28px !important;
    }
}
/* AETHEROS FINAL FIXED HEADER SYSTEM END */

/* AETHEROS LAYOUT COMPACTION WEEK1 COLOURS V3 START */
:root {
    --max: 1180px;

    --section-pad-y: 2.65rem;
    --section-pad-y-page: 3.2rem;
    --section-pad-y-hero: 4rem;

    --section-gap: 1.25rem;
    --grid-gap: 1.1rem;
    --card-pad: 1.45rem;

    --week1-gold: #FFD36A;
    --week1-gold-bg: rgba(255, 211, 106, 0.13);
    --week1-gold-border: rgba(255, 211, 106, 0.72);
    --week1-gold-glow: rgba(255, 211, 106, 0.18);

    --week1-blue: #5FA8FF;
    --week1-blue-bg: rgba(95, 168, 255, 0.13);
    --week1-blue-border: rgba(95, 168, 255, 0.66);
    --week1-blue-glow: rgba(95, 168, 255, 0.16);

    --week1-purple: #A77CFF;
    --week1-purple-bg: rgba(167, 124, 255, 0.13);
    --week1-purple-border: rgba(167, 124, 255, 0.66);
    --week1-purple-glow: rgba(167, 124, 255, 0.16);
}

.wrap,
.container {
    width: min(calc(100% - 32px), var(--max)) !important;
}

section {
    padding-top: var(--section-pad-y) !important;
    padding-bottom: var(--section-pad-y) !important;
}

main > section + section {
    padding-top: 2.15rem !important;
}

.page-hero,
section.page-hero {
    padding-top: var(--section-pad-y-hero) !important;
    padding-bottom: 2.95rem !important;
}

.hero,
.home-hero {
    min-height: 500px !important;
}

.hero .inner,
.home-hero .inner,
.hero .wrap,
.home-hero .wrap {
    padding-top: 3.1rem !important;
    padding-bottom: 3.1rem !important;
}

.hero-logo,
.home-hero-logo,
.page-logo,
.hero-icon,
.aetheros-fixed-hero-icon {
    margin-top: 0 !important;
    margin-bottom: 0.85rem !important;
}

.section-head {
    margin-bottom: 1.2rem !important;
}

.section-head h2,
.section-head h3 {
    margin-bottom: 0.55rem !important;
}

.section-head p,
.lead,
.hero p {
    max-width: 960px !important;
}

.grid,
.cards,
.cards-2,
.cards-3,
.cards-5 {
    gap: var(--grid-gap) !important;
}

.split {
    gap: 1.25rem !important;
}

.card,
.panel,
.notice,
.glass,
.paper-card {
    padding: var(--card-pad) !important;
}

.card h2,
.card h3,
.panel h2,
.panel h3,
.notice h2,
.notice h3 {
    margin-top: 0 !important;
    margin-bottom: 0.65rem !important;
}

.card p,
.panel p,
.notice p {
    margin-top: 0.55rem !important;
}

.card p:last-child,
.panel p:last-child,
.notice p:last-child {
    margin-bottom: 0 !important;
}

.actions,
.hero-actions,
.paper-actions,
.cta-actions {
    margin-top: 1.05rem !important;
    gap: 0.8rem !important;
}

.banner {
    margin-bottom: 1.1rem !important;
}

body.fellows-page .fellows-centred-midsection {
    display: block !important;
    text-align: center !important;
}

body.fellows-page .fellows-centred-midsection .wrap {
    display: flex !important;
    justify-content: center !important;
    align-items: stretch !important;
    width: min(calc(100% - 32px), var(--max)) !important;
    margin-left: auto !important;
    margin-right: auto !important;
}

body.fellows-page .fellows-centred-midsection .split,
body.fellows-page .fellows-centred-midsection .grid,
body.fellows-page .fellows-centred-midsection .cards,
body.fellows-page .fellows-centred-midsection .cards-2,
body.fellows-page .fellows-centred-midsection .cards-3 {
    display: grid !important;
    grid-template-columns: minmax(280px, 560px) !important;
    justify-content: center !important;
    justify-items: center !important;
    align-items: stretch !important;
    width: 100% !important;
    max-width: 620px !important;
    margin-left: auto !important;
    margin-right: auto !important;
}

body.fellows-page .fellows-centred-midsection .card,
body.fellows-page .fellows-centred-midsection .panel,
body.fellows-page .fellows-centred-midsection .notice {
    width: 100% !important;
    max-width: 560px !important;
    margin-left: auto !important;
    margin-right: auto !important;
    text-align: center !important;
}

body.fellows-page section {
    text-align: center;
}

/* NSRP page receives a slightly tighter rhythm because it has the densest section stack. */
body.nsrp-page section,
body[class*="nsrp"] section,
.nsrp-page section {
    padding-top: 2.25rem !important;
    padding-bottom: 2.25rem !important;
}

body.nsrp-page .page-hero,
body[class*="nsrp"] .page-hero,
.nsrp-page .page-hero {
    padding-top: 3.6rem !important;
    padding-bottom: 2.6rem !important;
}

.nsrp-prelude-moved {
    margin-top: 1.2rem !important;
    margin-bottom: 1.75rem !important;
    text-align: center !important;
}

.nsrp-prelude-moved .wrap,
.nsrp-prelude-moved .card,
.nsrp-prelude-moved .panel,
.nsrp-prelude-moved .notice {
    text-align: center !important;
    margin-left: auto !important;
    margin-right: auto !important;
}

/* Week 1 colour correction:
   Gold = announcement, Blue = read online, Purple = PDF download. */
.status-badge.complete,
.status-badge.paper-available,
.status-badge.first-paper,
.badge.paper-available,
.badge.first-paper,
.badge:where([class*="available"]),
[class*="badge"]:where([class*="available"]),
[class*="status"]:where([class*="available"]),
span:has(> strong):where(.badge, .status-badge),
a:not([href]):has-text("FIRST PAPER AVAILABLE") {
    color: var(--week1-gold) !important;
    background: var(--week1-gold-bg) !important;
    border-color: var(--week1-gold-border) !important;
    box-shadow: 0 0 20px var(--week1-gold-glow) !important;
}

a[href*="week01_wave_particle_duality.html"],
a[href*="week01_wave_particle_duality"][href$=".html"],
a[href*="papers/week01_wave_particle_duality.html"],
a[href*="week01"]:not([href*=".pdf"]) {
    color: #eaf5ff !important;
    background: var(--week1-blue-bg) !important;
    border-color: var(--week1-blue-border) !important;
    box-shadow: 0 0 20px var(--week1-blue-glow) !important;
}

a[href*="week01_wave_particle_duality.pdf"],
a[href*="week01"][href*=".pdf"],
a[href$=".pdf"] {
    color: #f3ecff !important;
    background: var(--week1-purple-bg) !important;
    border-color: var(--week1-purple-border) !important;
    box-shadow: 0 0 20px var(--week1-purple-glow) !important;
}

a[href*="week01_wave_particle_duality.html"]:hover,
a[href*="week01_wave_particle_duality.pdf"]:hover,
a[href$=".pdf"]:hover {
    transform: translateY(-1px) !important;
    filter: brightness(1.08) !important;
}

.footer,
footer {
    padding-top: 1.85rem !important;
    padding-bottom: 1.85rem !important;
}

@media (max-width: 900px) {
    :root {
        --section-pad-y: 2.25rem;
        --section-pad-y-page: 2.75rem;
        --section-pad-y-hero: 3.4rem;
        --grid-gap: 0.95rem;
        --card-pad: 1.25rem;
    }

    .hero,
    .home-hero {
        min-height: 460px !important;
    }

    .hero .inner,
    .home-hero .inner,
    .hero .wrap,
    .home-hero .wrap {
        padding-top: 2.55rem !important;
        padding-bottom: 2.55rem !important;
    }

    .split {
        gap: 1rem !important;
    }

    body.fellows-page .fellows-centred-midsection .split {
        grid-template-columns: 1fr !important;
    }
}

@media (max-width: 560px) {
    :root {
        --section-pad-y: 1.9rem;
        --section-pad-y-page: 2.25rem;
        --section-pad-y-hero: 2.85rem;
        --grid-gap: 0.8rem;
        --card-pad: 1.05rem;
    }

    .hero,
    .home-hero {
        min-height: 420px !important;
    }

    .hero .inner,
    .home-hero .inner,
    .hero .wrap,
    .home-hero .wrap {
        padding-top: 2rem !important;
        padding-bottom: 2rem !important;
    }

    .section-head {
        margin-bottom: 1rem !important;
    }

    .actions,
    .hero-actions,
    .paper-actions,
    .cta-actions {
        gap: 0.65rem !important;
    }
}
/* AETHEROS LAYOUT COMPACTION WEEK1 COLOURS V3 END */

/* AETHEROS WEEK1 DIRECT HOOKS START */
.paper-available,
.first-paper {
    color: var(--week1-gold) !important;
    background: var(--week1-gold-bg) !important;
    border-color: var(--week1-gold-border) !important;
    box-shadow: 0 0 20px var(--week1-gold-glow) !important;
}

.read-online-cta,
.week1-blue {
    color: #eaf5ff !important;
    background: var(--week1-blue-bg) !important;
    border-color: var(--week1-blue-border) !important;
    box-shadow: 0 0 20px var(--week1-blue-glow) !important;
}

.download-pdf-cta,
.week1-purple {
    color: #f3ecff !important;
    background: var(--week1-purple-bg) !important;
    border-color: var(--week1-purple-border) !important;
    box-shadow: 0 0 20px var(--week1-purple-glow) !important;
}
/* AETHEROS WEEK1 DIRECT HOOKS END */

/* AETHEROS CONTACT ENQUIRY PAGE REFLOW START */
.contact-flow-section {
    padding-top: 2.25rem !important;
    padding-bottom: 2.75rem !important;
}

.contact-flow-grid {
    display: grid !important;
    grid-template-columns: 1fr !important;
    gap: 1.15rem !important;
    max-width: 980px !important;
    margin-left: auto !important;
    margin-right: auto !important;
}

.contact-flow-card,
.contact-final-card,
.enquiry-form-card {
    text-align: center !important;
    max-width: 900px !important;
    margin-left: auto !important;
    margin-right: auto !important;
}

.contact-flow-card p,
.enquiry-form-card p {
    max-width: 760px !important;
    margin-left: auto !important;
    margin-right: auto !important;
}

.contact-enquiry-actions,
.enquiry-actions {
    justify-content: center !important;
    margin-top: 1.15rem !important;
}

.contact-enquiry-button,
.enquiry-submit-button {
    color: #eaf5ff !important;
    background: rgba(95, 168, 255, 0.13) !important;
    border-color: rgba(95, 168, 255, 0.66) !important;
    box-shadow: 0 0 20px rgba(95, 168, 255, 0.16) !important;
}

.enquiry-back-button {
    color: #f3ecff !important;
    background: rgba(167, 124, 255, 0.13) !important;
    border-color: rgba(167, 124, 255, 0.58) !important;
}

.enquiry-form-section {
    padding-top: 2.25rem !important;
    padding-bottom: 3rem !important;
}

.symtech-enquiry-form {
    display: grid !important;
    gap: 1rem !important;
    max-width: 760px !important;
    margin: 1.5rem auto 0 auto !important;
    text-align: left !important;
}

.symtech-enquiry-form label {
    display: grid !important;
    gap: 0.45rem !important;
    color: rgba(235, 242, 255, 0.92) !important;
    font-weight: 700 !important;
}

.symtech-enquiry-form input,
.symtech-enquiry-form select,
.symtech-enquiry-form textarea {
    width: 100% !important;
    box-sizing: border-box !important;
    border-radius: 14px !important;
    border: 1px solid rgba(95, 168, 255, 0.38) !important;
    background: rgba(5, 10, 22, 0.72) !important;
    color: #eef5ff !important;
    padding: 0.85rem 0.95rem !important;
    font: inherit !important;
    outline: none !important;
}

.symtech-enquiry-form input:focus,
.symtech-enquiry-form select:focus,
.symtech-enquiry-form textarea:focus {
    border-color: rgba(95, 168, 255, 0.82) !important;
    box-shadow: 0 0 0 3px rgba(95, 168, 255, 0.14) !important;
}

.enquiry-intro,
.contact-note {
    opacity: 0.82 !important;
    font-size: 0.96rem !important;
}

@media (max-width: 640px) {
    .symtech-enquiry-form {
        gap: 0.85rem !important;
    }

    .enquiry-actions {
        flex-direction: column !important;
        align-items: stretch !important;
    }
}
/* AETHEROS CONTACT ENQUIRY PAGE REFLOW END */

/* AETHEROS REAL ENQUIRY FORM SUBMIT START */
.enquiry-form-card {
    text-align: center !important;
}

.symtech-enquiry-form {
    display: grid !important;
    gap: 1rem !important;
    max-width: 760px !important;
    margin: 1.5rem auto 0 auto !important;
    text-align: left !important;
}

.symtech-enquiry-form label {
    display: grid !important;
    gap: 0.45rem !important;
}

.symtech-enquiry-form textarea {
    margin-bottom: 0.3rem !important;
}

.enquiry-send-row {
    justify-content: center !important;
    margin-top: 0.35rem !important;
    margin-bottom: 0.55rem !important;
}

.enquiry-submit-button {
    min-width: 150px !important;
    color: #eaf5ff !important;
    background: rgba(95, 168, 255, 0.13) !important;
    border-color: rgba(95, 168, 255, 0.72) !important;
    box-shadow: 0 0 20px rgba(95, 168, 255, 0.16) !important;
}

.enquiry-secondary-actions {
    justify-content: center !important;
    margin-top: 0.1rem !important;
}

.enquiry-back-button {
    color: #f3ecff !important;
    background: rgba(167, 124, 255, 0.13) !important;
    border-color: rgba(167, 124, 255, 0.58) !important;
}

.enquiry-submit-button:hover,
.enquiry-back-button:hover {
    transform: translateY(-1px) !important;
    filter: brightness(1.08) !important;
}
/* AETHEROS REAL ENQUIRY FORM SUBMIT END */

/* AETHEROS CTA STANDARDISATION START */
:root {
    --cta-primary-a: #5fa8ff;
    --cta-primary-b: #8d6dff;
    --cta-primary-text: #ffffff;
    --cta-secondary-text: #f3ecff;
    --cta-secondary-border: rgba(167, 124, 255, 0.66);
    --cta-secondary-bg: rgba(167, 124, 255, 0.13);
    --cta-secondary-glow: rgba(167, 124, 255, 0.18);
    --cta-link: #5ff0ff;
    --cta-link-glow: rgba(95, 240, 255, 0.28);
}

a.button,
button.button,
.btn-primary,
.symtech-cta-primary,
.contact-enquiry-button,
.enquiry-submit-button {
    display: inline-flex !important;
    align-items: center !important;
    justify-content: center !important;
    min-height: 46px !important;
    min-width: 168px !important;
    padding: 0.78rem 1.55rem !important;
    border-radius: 999px !important;
    border: 1px solid rgba(120, 178, 255, 0.72) !important;
    background: linear-gradient(135deg, var(--cta-primary-a), var(--cta-primary-b)) !important;
    color: var(--cta-primary-text) !important;
    text-decoration: none !important;
    font-weight: 800 !important;
    letter-spacing: 0.01em !important;
    line-height: 1.1 !important;
    box-shadow: 0 0 22px rgba(95, 168, 255, 0.22), inset 0 0 18px rgba(255, 255, 255, 0.08) !important;
    cursor: pointer !important;
    transition: transform 0.18s ease, filter 0.18s ease, box-shadow 0.18s ease !important;
}

a.button:hover,
button.button:hover,
.btn-primary:hover,
.symtech-cta-primary:hover,
.contact-enquiry-button:hover,
.enquiry-submit-button:hover {
    transform: translateY(-1px) !important;
    filter: brightness(1.08) !important;
    box-shadow: 0 0 28px rgba(95, 168, 255, 0.32), inset 0 0 20px rgba(255, 255, 255, 0.1) !important;
}

a.button.secondary,
button.button.secondary,
.btn-secondary,
.symtech-cta-secondary,
.enquiry-back-button {
    display: inline-flex !important;
    align-items: center !important;
    justify-content: center !important;
    min-height: 44px !important;
    min-width: 160px !important;
    padding: 0.72rem 1.45rem !important;
    border-radius: 999px !important;
    border: 1px solid var(--cta-secondary-border) !important;
    background: var(--cta-secondary-bg) !important;
    color: var(--cta-secondary-text) !important;
    text-decoration: none !important;
    font-weight: 800 !important;
    line-height: 1.1 !important;
    box-shadow: 0 0 20px var(--cta-secondary-glow), inset 0 0 14px rgba(255, 255, 255, 0.05) !important;
    cursor: pointer !important;
    transition: transform 0.18s ease, filter 0.18s ease, box-shadow 0.18s ease !important;
}

a.button.secondary:hover,
button.button.secondary:hover,
.btn-secondary:hover,
.symtech-cta-secondary:hover,
.enquiry-back-button:hover {
    transform: translateY(-1px) !important;
    filter: brightness(1.08) !important;
    box-shadow: 0 0 26px rgba(167, 124, 255, 0.26), inset 0 0 16px rgba(255, 255, 255, 0.08) !important;
}

.link-primary,
.symtech-text-link {
    display: inline-flex !important;
    align-items: center !important;
    gap: 0.25rem !important;
    color: var(--cta-link) !important;
    background: transparent !important;
    border: 0 !important;
    box-shadow: none !important;
    text-decoration: none !important;
    font-weight: 800 !important;
    letter-spacing: 0.02em !important;
    padding: 0.2rem 0 !important;
    transition: transform 0.18s ease, text-shadow 0.18s ease, color 0.18s ease !important;
}

.link-primary:hover,
.symtech-text-link:hover {
    transform: translateX(2px) !important;
    text-shadow: 0 0 14px var(--cta-link-glow) !important;
    color: #a6f7ff !important;
}

.enquiry-send-row,
.enquiry-secondary-actions,
.contact-enquiry-actions,
.actions,
.hero-actions {
    display: flex !important;
    align-items: center !important;
    justify-content: center !important;
    gap: 0.85rem !important;
    flex-wrap: wrap !important;
}

.enquiry-send-row {
    margin-top: 0.85rem !important;
    margin-bottom: 0.35rem !important;
}

.enquiry-secondary-actions {
    margin-top: 0.3rem !important;
}

@media (max-width: 560px) {
    a.button,
    button.button,
    .btn-primary,
    .symtech-cta-primary,
    .btn-secondary,
    .symtech-cta-secondary {
        width: min(100%, 280px) !important;
    }

    .actions,
    .hero-actions,
    .enquiry-actions,
    .enquiry-secondary-actions {
        flex-direction: column !important;
    }
}
/* AETHEROS CTA STANDARDISATION END */

/* AETHEROS PAPER PAGE LINKED IMAGE REBUILD START */
.paper-content-section {
    padding-top: 2.4rem !important;
    padding-bottom: 3rem !important;
}

.paper-article {
    max-width: 920px !important;
    margin-left: auto !important;
    margin-right: auto !important;
    line-height: 1.72 !important;
}

.paper-article h1,
.paper-article h2,
.paper-article h3,
.paper-article h4 {
    margin-top: 2.1rem !important;
    margin-bottom: 0.85rem !important;
}

.paper-article h1:first-child,
.paper-article h2:first-child {
    margin-top: 0 !important;
}

.paper-article p {
    margin-top: 0.85rem !important;
    margin-bottom: 0.85rem !important;
}

.paper-figure {
    margin: 1.8rem auto !important;
    text-align: center !important;
}

.paper-figure img {
    width: min(100%, 860px) !important;
    height: auto !important;
    border-radius: 18px !important;
    border: 1px solid rgba(95, 168, 255, 0.22) !important;
    box-shadow: 0 0 30px rgba(95, 168, 255, 0.10) !important;
}

.paper-figure figcaption {
    margin-top: 0.7rem !important;
    color: rgba(225, 235, 255, 0.72) !important;
    font-style: italic !important;
    font-size: 0.95rem !important;
}

@media (max-width: 700px) {
    .paper-article {
        line-height: 1.62 !important;
    }

    .paper-figure {
        margin: 1.35rem auto !important;
    }
}
/* AETHEROS PAPER PAGE LINKED IMAGE REBUILD END */

/* AETHEROS HERO KICKER ALIGNMENT START */
.hero,
.home-hero,
.page-hero,
.hero .wrap,
.home-hero .wrap,
.page-hero .wrap,
.hero .inner,
.home-hero .inner,
.page-hero .inner {
    text-align: center !important;
}

.kicker,
.hero-kicker,
.eyebrow,
.hero-eyebrow,
.page-kicker,
.home-kicker,
.home-hero .kicker,
.hero .kicker,
.page-hero .kicker {
    display: block !important;
    width: 100% !important;
    max-width: none !important;
    margin-left: auto !important;
    margin-right: auto !important;
    text-align: center !important;
    justify-content: center !important;
    align-items: center !important;
}

.home-hero .kicker,
.hero .kicker,
.page-hero .kicker {
    transform: none !important;
    left: auto !important;
    right: auto !important;
}

.hero h1,
.home-hero h1,
.page-hero h1,
.hero .lead,
.home-hero .lead,
.page-hero .lead,
.hero p,
.home-hero p,
.page-hero p {
    text-align: center !important;
    margin-left: auto !important;
    margin-right: auto !important;
}

.hero-actions,
.hero .actions,
.home-hero .actions,
.page-hero .actions {
    justify-content: center !important;
}
/* AETHEROS HERO KICKER ALIGNMENT END */

/* AETHEROS CONTACT ENQUIRY KICKER FIX START */
.page-hero .kicker,
.home-hero .kicker,
.hero .kicker,
.contact-hero .kicker,
.enquiry-hero .kicker,
body.contact-page .page-hero .kicker,
body.enquiry-page .page-hero .kicker,
body.contact-page .kicker,
body.enquiry-page .kicker {
    display: block !important;
    width: 100% !important;
    margin-left: auto !important;
    margin-right: auto !important;
    text-align: center !important;
    color: #5ff3ff !important;
    text-transform: uppercase !important;
    letter-spacing: 0.22em !important;
    font-weight: 800 !important;
    font-size: clamp(0.78rem, 1.1vw, 1rem) !important;
    line-height: 1.35 !important;
    text-shadow: 0 0 18px rgba(95, 243, 255, 0.38) !important;
}
/* AETHEROS CONTACT ENQUIRY KICKER FIX END */


/* AetherOS index SEO/layout rhythm patch v1 */
main > .home-section {
  padding-block: clamp(3.5rem, 7vw, 6rem);
}

main > .home-section + .home-section {
  margin-top: 0;
}

.home-section .section-head,
.home-section .panel,
.home-section .notice {
  margin-inline: auto;
}

.home-purpose .section-head,
.home-ecosystem .section-head {
  max-width: 860px;
  text-align: center;
}

.home-founder .panel,
.home-status .panel,
.home-contact .notice {
  max-width: 920px;
}

.home-ecosystem .grid {
  margin-top: clamp(1.75rem, 4vw, 3rem);
}

.home-status .split {
  align-items: center;
  gap: clamp(2rem, 5vw, 4rem);
}

/* AetherOS homepage hero image reorder patch */
.homepage-hero-image-section {
  padding: 2.75rem 0 2.25rem;
}

.homepage-hero-image {
  display: block;
  width: min(100%, 1180px);
  margin: 0 auto;
  border-radius: 26px;
  box-shadow: 0 28px 80px rgba(0, 0, 0, 0.38);
  object-fit: cover;
}

.homepage-section {
  padding: 3.25rem 0;
}

.homepage-section .section-head {
  margin-bottom: 2rem;
}

.homepage-founder-section,
.homepage-status-section {
  padding-top: 2.75rem;
  padding-bottom: 2.75rem;
}

.homepage-contact-section {
  padding-bottom: 4rem;
}

@media (max-width: 760px) {
  .homepage-hero-image-section {
    padding: 1.75rem 0 1.5rem;
  }

  .homepage-section {
    padding: 2.25rem 0;
  }

  .homepage-hero-image {
    border-radius: 18px;
  }
}

/* AETHEROS_STANDARD_VISUAL_HERO_RHYTHM_START */
.page-hero {
  padding-bottom: 2.75rem !important;
}

.page-hero + .page-visual-hero {
  padding-top: 0 !important;
}

.page-visual-hero {
  padding-top: 0 !important;
  padding-bottom: 3rem !important;
  margin: 0 !important;
  background: transparent !important;
}

.page-visual-hero .wrap {
  width: min(calc(100% - 32px), var(--max)) !important;
  max-width: 1120px !important;
  margin-left: auto !important;
  margin-right: auto !important;
}

.hero-image-frame {
  display: block !important;
  width: 100% !important;
  margin: 0 auto !important;
  overflow: hidden !important;
  border-radius: 28px !important;
  border: 1px solid rgba(135, 206, 250, 0.24) !important;
  background: rgba(5, 16, 32, 0.62) !important;
  box-shadow: 0 24px 70px rgba(0, 0, 0, 0.42), 0 0 42px rgba(71, 170, 255, 0.16) !important;
  aspect-ratio: 16 / 9 !important;
}

.hero-image-frame img {
  display: block !important;
  width: 100% !important;
  height: 100% !important;
  max-width: none !important;
  object-fit: cover !important;
  object-position: center center !important;
  aspect-ratio: auto !important;
}

.page-visual-hero + section {
  padding-top: 0 !important;
}

body.fellows-page .fellows-centred-midsection {
  padding-top: 0 !important;
}

body.fellows-page .fellows-centred-midsection + .page-visual-hero {
  display: none !important;
}

@media (max-width: 760px) {
  .page-hero {
    padding-bottom: 2rem !important;
  }

  .page-visual-hero {
    padding-bottom: 2.2rem !important;
  }

  .hero-image-frame {
    border-radius: 18px !important;
  }
}
/* AETHEROS_STANDARD_VISUAL_HERO_RHYTHM_END */

/* AETHEROS_CONTACT_HERO_DIVIDER_FIX_START */
.contact-page .contact-hero,
.contact-page .page-hero.contact-hero {
    border-bottom: 0 !important;
    box-shadow: none !important;
}

.contact-page .contact-hero::after,
.contact-page .page-hero.contact-hero::after {
    display: none !important;
    content: none !important;
}

.contact-page .contact-flow-section {
    border-top: 0 !important;
}
/* AETHEROS_CONTACT_HERO_DIVIDER_FIX_END */

